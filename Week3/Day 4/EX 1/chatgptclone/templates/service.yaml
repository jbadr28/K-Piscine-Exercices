apiVersion: v1
kind: Service
metadata:
  name: {{ include "chatgptclone.fullname" . }}
  labels:
    {{- include "chatgptclone.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chatgptclone.selectorLabels" . | nindent 4 }}
